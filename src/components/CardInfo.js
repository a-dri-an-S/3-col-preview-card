const CardInfo = ({ title, info }) => {
    return (
        <div className="">
            <h1 className="">{title}</h1>
            <p className="">{info}</p>
        </div>
    );
}

export default CardInfo;